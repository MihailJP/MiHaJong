/*=============================
 * HSP麻雀クライアントMiHaJong
 *     [ルール表示ルーチン]
 *=============================
 */

#deffunc chatshowrule var RuleList
	configtxt
	repeat NUM_OF_RULES
		if ((RuleName(cnt) != "")&&(RuleTxt(0, cnt) != "Ｎ／Ａ")) {
			RuleList += strf("%s: %s%s", RuleName(cnt), RuleTxt(Rule(cnt), cnt), "\n")
		}
	loop
#if 0
return
